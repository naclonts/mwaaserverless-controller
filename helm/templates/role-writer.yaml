---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  creationTimestamp: null
  name: {{ include "ack-mwaaserverless-controller.app.fullname" . }}-writer
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "ack-mwaaserverless-controller.app.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    k8s-app: {{ include "ack-mwaaserverless-controller.app.name" . }}
    helm.sh/chart: {{ include "ack-mwaaserverless-controller.chart.name-version" . }}
rules:
- apiGroups:
  - mwaaserverless.services.k8s.aws
  resources:
  - workflows
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - mwaaserverless.services.k8s.aws
  resources:
  - workflows
  verbs:
  - get
  - patch
  - update
